# Backend origin only (no /api). The app always calls `${origin}/api/...` to match Express routes.
# Legacy: `http://localhost:4000/api` is also accepted and normalized to the same origin.
VITE_API_BASE_URL=http://localhost:4000
